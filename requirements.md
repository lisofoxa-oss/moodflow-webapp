## Packages
recharts | For visualizing mood distribution stats
framer-motion | For smooth animations and emoji rain effects
date-fns | For formatting timestamps

## Notes
WebSocket connection required at location.protocol + '//' + location.host (auto-detects ws/wss)
Dashboard receives real-time updates via WS: viewers_count, new_mood, stats_update
